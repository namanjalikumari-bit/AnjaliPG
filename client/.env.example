# Frontend env. In local dev the Vite proxy forwards /api -> http://localhost:5000,
# so you usually DON'T need this. Set it for production (point to your deployed API).
VITE_API_URL=https://your-backend.onrender.com/api
